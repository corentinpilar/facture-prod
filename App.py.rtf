{\rtf1\ansi\ansicpg1252\cocoartf2867
\cocoatextscaling0\cocoaplatform0{\fonttbl\f0\fswiss\fcharset0 Helvetica;}
{\colortbl;\red255\green255\blue255;}
{\*\expandedcolortbl;;}
\paperw11900\paperh16840\margl1440\margr1440\vieww11520\viewh8400\viewkind0
\pard\tx720\tx1440\tx2160\tx2880\tx3600\tx4320\tx5040\tx5760\tx6480\tx7200\tx7920\tx8640\pardirnatural\partightenfactor0

\f0\fs24 \cf0 import streamlit as st\
import json\
import io\
import os\
import zipfile\
from datetime import datetime\
from pypdf import PdfReader, PdfWriter\
\
st.set_page_config(page_title="PDF Manager Prod", page_icon="\uc0\u55356 \u57260 ")\
\
st.title("\uc0\u55356 \u57260  PDF Manager - Admin Production")\
st.write("Outil de fusion et d\'e9coupage des factures pour signature.")\
\
tab1, tab2 = st.tabs(["\uc0\u10133  Fusionner (Avant signature)", "\u9986 \u65039  D\'e9couper (Apr\'e8s signature)"])\
\
# --- FUSION ---\
with tab1:\
    files = st.file_uploader("Glissez les PDF \'e0 combiner", type="pdf", accept_multiple_files=True)\
    if files:\
        if st.button("G\'e9n\'e9rer le PDF pour le DP"):\
            writer = PdfWriter()\
            carte = []\
            page_cursor = 1\
            for f in sorted(files, key=lambda x: x.name):\
                reader = PdfReader(f)\
                writer.append(f)\
                n_pages = len(reader.pages)\
                carte.append(\{"nom": f.name, "debut": page_cursor, "fin": page_cursor + n_pages - 1\})\
                page_cursor += n_pages\
            \
            pdf_out = io.BytesIO()\
            writer.write(pdf_out)\
            ts = datetime.now().strftime("%Y%m%d_%H%M")\
            \
            st.success("Fusion termin\'e9e !")\
            st.download_button("\uc0\u11015 \u65039  T\'e9l\'e9charger le PDF", data=pdf_out.getvalue(), file_name=f"POUR_SIGNATURE_\{ts\}.pdf")\
            st.download_button("\uc0\u11015 \u65039  T\'e9l\'e9charger le fichier JSON (IMPORTANT)", data=json.dumps(carte), file_name=f"CARTE_\{ts\}.json")\
\
# --- DECOUPAGE ---\
with tab2:\
    signed_pdf = st.file_uploader("Le PDF sign\'e9 par le DP", type="pdf")\
    json_file = st.file_uploader("Le fichier JSON correspondant", type="json")\
    if signed_pdf and json_file:\
        if st.button("Extraire les factures individuelles"):\
            carte = json.load(json_file)\
            reader = PdfReader(signed_pdf)\
            last_page = reader.pages[-1]\
            zip_out = io.BytesIO()\
            with zipfile.ZipFile(zip_out, "w") as zf:\
                for e in carte:\
                    sw = PdfWriter()\
                    for i in range(e["debut"]-1, e["fin"]):\
                        sw.add_page(reader.pages[i])\
                    sw.add_page(last_page)\
                    buf = io.BytesIO()\
                    sw.write(buf)\
                    zf.writestr(e["nom"].replace(".pdf", " (signed).pdf"), buf.getvalue())\
            \
            st.success("Extraction termin\'e9e !")\
            st.download_button("\uc0\u11015 \u65039  T\'e9l\'e9charger toutes les pi\'e8ces (ZIP)", data=zip_out.getvalue(), file_name="factures_signees.zip")}